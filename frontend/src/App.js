import React from 'react';

export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Job Portal</h1>
      <p>React frontend (production build served by Nginx).</p>
    </main>
  );
}
